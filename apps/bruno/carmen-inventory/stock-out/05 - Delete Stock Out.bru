meta {
  name: 05 - Delete Stock Out
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/stock-out/:id
  body: none
  auth: bearer
}

params:path {
  id: {{stock_out_id}}
}

auth:bearer {
  token: {{access_token}}
}
