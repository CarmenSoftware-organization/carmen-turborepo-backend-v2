meta {
  name: 05 - Delete Period
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/period/:id
  body: none
  auth: bearer
}

params:path {
  id: {{period_id}}
}

auth:bearer {
  token: {{access_token}}
}
