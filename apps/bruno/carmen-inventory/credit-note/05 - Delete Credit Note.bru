meta {
  name: 05 - Delete Credit Note
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/credit-note/:id
  body: none
  auth: bearer
}

params:path {
  id: {{credit_note_id}}
}

auth:bearer {
  token: {{access_token}}
}
