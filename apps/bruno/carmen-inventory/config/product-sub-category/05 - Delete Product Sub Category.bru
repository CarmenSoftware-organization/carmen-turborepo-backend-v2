meta {
  name: 05 - Delete Product Sub Category
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/products/sub-category/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
