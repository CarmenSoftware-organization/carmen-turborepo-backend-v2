meta {
  name: 06 - Delete Currency
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/currencies/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
