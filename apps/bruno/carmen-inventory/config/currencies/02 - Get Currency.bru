meta {
  name: 02 - Get Currency
  type: http
  seq: 2
}

docs {
  ## Get Currency
  ดึงข้อมูลสกุลเงินรายการเดียวตาม ID

  Retrieves a single currency configuration by its unique identifier.

  ### Path Parameters
  - `id` -- Currency ID (UUID) / รหัสสกุลเงิน
}

get {
  url: {{host}}/api/config/{{bu_code}}/currencies/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
