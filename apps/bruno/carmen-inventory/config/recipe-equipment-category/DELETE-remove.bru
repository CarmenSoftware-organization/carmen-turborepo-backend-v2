meta {
  name: Remove config_recipe-equipment-category
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment-category/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
