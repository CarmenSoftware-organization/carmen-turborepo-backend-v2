meta {
  name: Update config_recipe-equipment-category
  type: http
  seq: 4
}

put {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment-category/:id
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
