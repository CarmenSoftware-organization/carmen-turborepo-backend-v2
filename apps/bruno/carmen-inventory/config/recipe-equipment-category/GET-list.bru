meta {
  name: List config_recipe-equipment-category
  type: http
  seq: 2
}

get {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment-category
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
