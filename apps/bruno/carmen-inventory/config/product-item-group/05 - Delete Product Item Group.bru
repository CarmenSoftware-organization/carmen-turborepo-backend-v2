meta {
  name: 05 - Delete Product Item Group
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/products/item-group/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
