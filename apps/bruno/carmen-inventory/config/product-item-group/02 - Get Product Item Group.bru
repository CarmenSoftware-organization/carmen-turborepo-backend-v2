meta {
  name: 02 - Get Product Item Group
  type: http
  seq: 2
}

docs {
  ## Get Product Item Group
  ดึงข้อมูลกลุ่มรายการสินค้ารายการเดียวตาม ID

  Retrieves a specific product item group by its ID.

  ### Path Parameters
  - `id` — Product Item Group UUID (รหัสกลุ่มรายการสินค้า)
}

get {
  url: {{host}}/api/config/{{bu_code}}/products/item-group/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
