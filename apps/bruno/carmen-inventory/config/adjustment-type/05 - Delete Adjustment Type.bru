meta {
  name: 05 - Delete Adjustment Type
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/adjustment-type/:id
  body: none
  auth: bearer
}

params:path {
  id: {{adjustment_type_id}}
}

auth:bearer {
  token: {{access_token}}
}
