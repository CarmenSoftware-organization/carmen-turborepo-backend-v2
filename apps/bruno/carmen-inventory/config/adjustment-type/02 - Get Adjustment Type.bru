meta {
  name: 02 - Get Adjustment Type
  type: http
  seq: 2
}

docs {
  ## Get Adjustment Type
  ดึงข้อมูลประเภทการปรับปรุงรายการเดียวตาม ID

  Retrieves a single adjustment type by its unique identifier.
}

get {
  url: {{host}}/api/config/{{bu_code}}/adjustment-type/:id
  body: none
  auth: bearer
}

params:path {
  id: {{adjustment_type_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
