meta {
  name: 02 - Get Adjustment Type
  type: http
  seq: 2
}

get {
  url: {{host}}/api/config/{{bu_code}}/adjustment-type/:id
  body: none
  auth: bearer
}

params:path {
  id: {{adjustment_type_id}}
}

auth:bearer {
  token: {{access_token}}
}
