meta {
  name: 05 - Delete Application Role
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/application-roles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
