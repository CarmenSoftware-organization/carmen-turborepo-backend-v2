meta {
  name: 05 - Delete Application Role
  type: http
  seq: 5
}

docs {
  ## Delete Application Role
  ลบบทบาทในระบบตาม ID

  Deletes an application role by its unique identifier.
}

delete {
  url: {{host}}/api/config/{{bu_code}}/application-roles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
