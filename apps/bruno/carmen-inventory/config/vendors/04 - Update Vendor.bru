meta {
  name: 04 - Update Vendor
  type: http
  seq: 4
}

docs {
  ## Update Vendor
  อัปเดตข้อมูลผู้ขายที่มีอยู่

  Updates an existing vendor/supplier record.
}

put {
  url: {{host}}/api/config/{{bu_code}}/vendors/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
