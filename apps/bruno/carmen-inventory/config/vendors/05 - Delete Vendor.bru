meta {
  name: 05 - Delete Vendor
  type: http
  seq: 5
}

docs {
  ## Delete Vendor
  ลบผู้ขายตาม ID

  Deletes a vendor/supplier by its unique identifier.
}

delete {
  url: {{host}}/api/config/{{bu_code}}/vendors/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
