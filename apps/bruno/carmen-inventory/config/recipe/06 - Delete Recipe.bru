meta {
  name: 06 - Delete Recipe
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
