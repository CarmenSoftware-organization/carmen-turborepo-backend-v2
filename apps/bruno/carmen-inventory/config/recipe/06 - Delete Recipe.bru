meta {
  name: Remove config_recipe
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

docs {
  ## Delete Recipe
  ลบสูตรอาหารออกจากแค็ตตาล็อก ข้อมูลต้นทุนในอดีตยังคงอยู่

  Removes a recipe from the active catalog. Historical cost data is preserved.


  ### Sample Response
  ```json
  {
    "data": {
      "id": "uuid"
    },
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-03-27T00:00:00.000Z"
  }
  ```
}

params:path {
  id: 
}
