meta {
  name: 06 - Delete Recipe
  type: http
  seq: 6
}

docs {
  ## Delete Recipe
  ลบสูตรอาหารออกจากแค็ตตาล็อก ข้อมูลต้นทุนในอดีตยังคงอยู่

  Removes a recipe from the active catalog. Historical cost data is preserved.
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
