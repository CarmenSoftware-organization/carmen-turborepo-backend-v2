meta {
  name: 01 - List Exchange Rate
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/exchange-rate
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
