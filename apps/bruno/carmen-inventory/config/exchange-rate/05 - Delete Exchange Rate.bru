meta {
  name: 05 - Delete Exchange Rate
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/exchange-rate/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
