meta {
  name: 04 - Delete User Application Role
  type: http
  seq: 4
}

delete {
  url: {{host}}/api/config/{{bu_code}}/user-application-roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
