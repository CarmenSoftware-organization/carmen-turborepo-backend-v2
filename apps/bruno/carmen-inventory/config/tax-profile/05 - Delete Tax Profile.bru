meta {
  name: Remove config_tax_profile
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/tax-profile/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

docs {
  ## Delete Tax Profile
  ลบโปรไฟล์ภาษีตาม ID

  Removes a tax rate configuration from active use.

  ### Path Parameters
  - `id` -- Tax profile ID (UUID) / รหัสโปรไฟล์ภาษี


  ### Sample Response
  ```json
  {
    "data": {
      "id": "uuid"
    },
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-03-27T00:00:00.000Z"
  }
  ```
}

params:path {
  id: 
}
