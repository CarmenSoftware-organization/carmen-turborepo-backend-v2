meta {
  name: 05 - Delete Tax Profile
  type: http
  seq: 5
}

docs {
  ## Delete Tax Profile
  ลบโปรไฟล์ภาษีตาม ID

  Removes a tax rate configuration from active use.

  ### Path Parameters
  - `id` -- Tax profile ID (UUID) / รหัสโปรไฟล์ภาษี
}

delete {
  url: {{host}}/api/config/{{bu_code}}/tax-profile/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
