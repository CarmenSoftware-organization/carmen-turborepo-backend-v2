meta {
  name: 02 - Get Tax Profile
  type: http
  seq: 2
}

get {
  url: {{host}}/api/config/{{bu_code}}/tax-profile/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
