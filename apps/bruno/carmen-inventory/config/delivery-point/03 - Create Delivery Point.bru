meta {
  name: 03 - Create Delivery Point
  type: http
  seq: 3
}

post {
  url: {{host}}/api/config/{{bu_code}}/delivery-point
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
