meta {
  name: 04 - Update Delivery Point
  type: http
  seq: 4
}

put {
  url: {{host}}/api/config/{{bu_code}}/delivery-point/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
