meta {
  name: 06 - Delete Delivery Point
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/delivery-point/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
