meta {
  name: 02 - Get Department User
  type: http
  seq: 2
}

docs {
  ## Get Department User
  ดึงข้อมูลผู้ใช้ประจำแผนกรายการเดียวตาม ID

  Retrieves a single department-user assignment by its unique identifier.
}

get {
  url: {{host}}/api/config/{{bu_code}}/department-user/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
