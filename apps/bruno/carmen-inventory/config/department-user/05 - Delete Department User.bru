meta {
  name: 05 - Delete Department User
  type: http
  seq: 5
}

docs {
  ## Delete Department User
  ลบผู้ใช้ประจำแผนกตาม ID

  Deletes a department-user assignment by its unique identifier.
}

delete {
  url: {{host}}/api/config/{{bu_code}}/department-user/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
