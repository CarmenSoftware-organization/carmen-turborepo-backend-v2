meta {
  name: 05 - Delete Price List
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/price-list/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
