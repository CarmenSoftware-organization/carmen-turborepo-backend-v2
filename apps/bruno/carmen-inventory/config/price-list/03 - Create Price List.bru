meta {
  name: 03 - Create Price List
  type: http
  seq: 3
}

docs {
  ## Create Price List (Config)
  สร้างรายการราคาใหม่

  Creates a new vendor price list with product-level pricing details.
}

post {
  url: {{host}}/api/config/{{bu_code}}/price-list
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
