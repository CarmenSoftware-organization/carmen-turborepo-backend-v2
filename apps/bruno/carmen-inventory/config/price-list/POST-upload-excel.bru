meta {
  name: Upload excel config_price-list
  type: http
  seq: 7
}

post {
  url: {{host}}/api/config/{{bu_code}}/price-list/upload-excel
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
