meta {
  name: Db objects config_sql_query
  type: http
  seq: 9
}

get {
  url: {{host}}/api/config/{{bu_code}}/sql-query/db-objects
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
