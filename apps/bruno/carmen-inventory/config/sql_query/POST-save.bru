meta {
  name: Save config_sql_query
  type: http
  seq: 8
}

post {
  url: {{host}}/api/config/{{bu_code}}/sql-query/save
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
