meta {
  name: Db object definition config_sql_query
  type: http
  seq: 10
}

get {
  url: {{host}}/api/config/{{bu_code}}/sql-query/db-objects/definition
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~type: 
  ~schema: 
  ~name: 
}
