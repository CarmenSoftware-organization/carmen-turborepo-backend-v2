meta {
  name: 02 - Update User Location
  type: http
  seq: 2
}

put {
  url: {{host}}/api/config/{{bu_code}}/user/location/:locationId
  body: json
  auth: bearer
}

params:path {
  locationId: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "user_ids": []
  }
}
