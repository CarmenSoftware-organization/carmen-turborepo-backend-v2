meta {
  name: 01 - List Recipe Equipment
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
