meta {
  name: 06 - Delete Recipe Equipment
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
