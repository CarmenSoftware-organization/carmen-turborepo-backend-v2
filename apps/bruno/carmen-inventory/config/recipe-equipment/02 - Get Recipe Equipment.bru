meta {
  name: 02 - Get Recipe Equipment
  type: http
  seq: 2
}

get {
  url: {{host}}/api/config/{{bu_code}}/recipe-equipment/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
