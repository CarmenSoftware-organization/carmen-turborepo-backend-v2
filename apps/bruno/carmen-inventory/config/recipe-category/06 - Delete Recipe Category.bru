meta {
  name: 06 - Delete Recipe Category
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/config/{{bu_code}}/recipe-category/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
