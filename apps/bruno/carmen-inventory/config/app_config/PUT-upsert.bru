meta {
  name: Upsert config_app_config
  type: http
  seq: 3
}

put {
  url: {{host}}/api/config/{{bu_code}}/app-config/:key
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
