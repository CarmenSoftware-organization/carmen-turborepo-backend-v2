meta {
  name: Test email config_app_config
  type: http
  seq: 5
}

post {
  url: {{host}}/api/config/{{bu_code}}/app-config/test-email
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
