meta {
  name: List config_app_config
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/app-config
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
