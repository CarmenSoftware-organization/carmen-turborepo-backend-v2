meta {
  name: 05 - Delete Credit Term
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/credit-term/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
