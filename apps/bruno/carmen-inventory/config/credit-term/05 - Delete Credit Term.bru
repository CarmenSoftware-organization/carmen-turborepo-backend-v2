meta {
  name: 05 - Delete Credit Term
  type: http
  seq: 5
}

docs {
  ## Delete Credit Term
  ลบเงื่อนไขการชำระเงินตาม ID

  Removes a payment term from active use.

  ### Path Parameters
  - `id` -- Credit term ID (UUID) / รหัสเงื่อนไขการชำระเงิน
}

delete {
  url: {{host}}/api/config/{{bu_code}}/credit-term/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
