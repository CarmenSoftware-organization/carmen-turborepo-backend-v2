meta {
  name: 03 - Create Department
  type: http
  seq: 3
}

docs {
  ## Create Department
  สร้างแผนกใหม่

  Creates a new department in the business unit.
}

post {
  url: {{host}}/api/config/{{bu_code}}/departments
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
