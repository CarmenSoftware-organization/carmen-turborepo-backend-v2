meta {
  name: 02 - Get Department
  type: http
  seq: 2
}

get {
  url: {{host}}/api/config/{{bu_code}}/departments/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
