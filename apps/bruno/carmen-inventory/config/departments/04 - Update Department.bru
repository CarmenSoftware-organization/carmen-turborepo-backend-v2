meta {
  name: 04 - Update Department
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/config/{{bu_code}}/departments/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
