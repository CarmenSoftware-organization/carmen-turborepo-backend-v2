meta {
  name: 05 - Delete Department
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/departments/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
