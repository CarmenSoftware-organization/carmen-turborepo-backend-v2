meta {
  name: 01 - Get Locations User
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/locations/user/:userId
  body: none
  auth: bearer
}

params:path {
  userId: 
}

auth:bearer {
  token: {{access_token}}
}
