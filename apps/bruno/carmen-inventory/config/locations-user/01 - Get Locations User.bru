meta {
  name: 01 - Get Locations User
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/locations/user/:userId
  body: none
  auth: bearer
}

params:path {
  userId: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
