meta {
  name: 02 - Update Locations User
  type: http
  seq: 2
}

put {
  url: {{host}}/api/config/{{bu_code}}/locations/user/:userId
  body: json
  auth: bearer
}

params:path {
  userId: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "location_ids": []
  }
}
