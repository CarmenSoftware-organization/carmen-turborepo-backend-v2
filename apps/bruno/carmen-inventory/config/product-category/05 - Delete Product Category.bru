meta {
  name: 05 - Delete Product Category
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/products/category/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
