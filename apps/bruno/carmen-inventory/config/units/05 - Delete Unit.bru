meta {
  name: 05 - Delete Unit
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/units/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
