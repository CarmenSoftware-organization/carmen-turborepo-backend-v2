meta {
  name: 05 - Delete Unit
  type: http
  seq: 5
}

docs {
  ## Delete Unit
  ลบหน่วยนับตาม ID

  Deletes a unit of measurement by its unique identifier.
}

delete {
  url: {{host}}/api/config/{{bu_code}}/units/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
