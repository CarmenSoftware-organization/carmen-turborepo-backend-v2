meta {
  name: 05 - Patch Recipe Cuisine
  type: http
  seq: 5
}

patch {
  url: {{host}}/api/config/{{bu_code}}/recipe-cuisine/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "is_active": true
  }
}
