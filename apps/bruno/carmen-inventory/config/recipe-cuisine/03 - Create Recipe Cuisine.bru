meta {
  name: Create config_recipe-cuisine
  type: http
  seq: 3
}

post {
  url: {{host}}/api/config/{{bu_code}}/recipe-cuisine
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}

docs {
  ## Create Recipe Cuisine
  สร้างประเภทอาหารใหม่สำหรับจัดหมวดหมู่สูตรอาหาร เช่น อาหารไทย อาหารอิตาเลียน

  Defines a new cuisine type for recipe classification (e.g., Mediterranean, Mexican) for organized menu planning.


  ### Sample Body
  ```json
  {
    "name": "",
    "code": "",
    "is_active": true
  }
  ```

  ### Sample Response
  ```json
  {
    "data": {
      "id": "uuid"
    },
    "paginate": null,
    "status": 201,
    "success": true,
    "message": "Success",
    "timestamp": "2026-03-27T00:00:00.000Z"
  }
  ```
}
