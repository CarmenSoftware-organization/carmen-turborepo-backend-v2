meta {
  name: 04 - Create Running Code
  type: http
  seq: 4
}

post {
  url: {{host}}/api/config/{{bu_code}}/running-code
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
