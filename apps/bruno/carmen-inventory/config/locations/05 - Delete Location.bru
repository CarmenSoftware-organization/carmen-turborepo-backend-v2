meta {
  name: 05 - Delete Location
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/locations/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{access_token}}
}
