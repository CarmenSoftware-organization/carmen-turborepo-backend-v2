meta {
  name: 05 - Update Product
  type: http
  seq: 5
}

patch {
  url: {{host}}/api/config/{{bu_code}}/products/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "code": "",
    "is_active": true
  }
}
