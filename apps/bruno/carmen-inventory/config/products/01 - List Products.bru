meta {
  name: 01 - List Products
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/products?search=Ground
  body: none
  auth: bearer
}

params:query {
  search: Ground
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
