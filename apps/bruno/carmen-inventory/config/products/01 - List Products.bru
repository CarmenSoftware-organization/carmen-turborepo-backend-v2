meta {
  name: 01 - List Products
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/products?perpage=-1
  body: none
  auth: bearer
}

params:query {
  perpage: -1
  ~search: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
