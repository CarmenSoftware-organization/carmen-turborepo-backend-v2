meta {
  name: 01 - List Cronjobs
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/cronjobs
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
