meta {
  name: 05 - Delete Cronjob
  type: http
  seq: 5
}

docs {
  ## Delete Cronjob
  ลบงานตั้งเวลาอย่างถาวรและหยุดการทำงาน

  Permanently removes a scheduled task from the system and stops it from executing.
}

delete {
  url: {{host}}/api/config/cronjobs/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
