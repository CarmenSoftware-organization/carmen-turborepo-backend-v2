meta {
  name: 02 - Get Department
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/department/:id
  body: none
  auth: bearer
}

params:path {
  id: {{department_id}}
}

auth:bearer {
  token: {{access_token}}
}
