meta {
  name: 05 - Delete Price List Template
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/price-list-template/:id
  body: none
  auth: bearer
}

params:path {
  id: {{price_list_template_id}}
}

auth:bearer {
  token: {{access_token}}
}
