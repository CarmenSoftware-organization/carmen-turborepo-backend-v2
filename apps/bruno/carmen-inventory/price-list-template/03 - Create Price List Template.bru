meta {
  name: 03 - Create Price List Template
  type: http
  seq: 4
}

post {
  url: {{host}}/api/{{bu_code}}/price-list-template
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "b",
    "description": ""
  }
}
