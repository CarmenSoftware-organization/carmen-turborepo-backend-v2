meta {
  name: 02 - Get Price List Template
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/price-list-template/:id
  body: none
  auth: bearer
}

params:path {
  id: {{price_list_template_id}}
}

auth:bearer {
  token: {{access_token}}
}
