meta {
  name: Find by id my-pending
  type: http
  seq: 13
}

get {
  url: {{host}}/api/my-pending/purchase-order/{{bu_code}}/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
