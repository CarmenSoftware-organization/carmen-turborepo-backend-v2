meta {
  name: 04 - Update GRN
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/{{bu_code}}/good-received-note/:id
  body: json
  auth: bearer
}

params:path {
  id: {{grn_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "",
    "note": ""
  }
}
