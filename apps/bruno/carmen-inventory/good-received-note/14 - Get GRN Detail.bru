meta {
  name: 14 - Get GRN Detail
  type: http
  seq: 14
}

get {
  url: {{host}}/api/{{bu_code}}/good-received-note/:id/details/:detail_id
  body: none
  auth: bearer
}

params:path {
  id: {{grn_id}}
  detail_id: {{grn_detail_id}}
}

auth:bearer {
  token: {{access_token}}
}
