meta {
  name: 12 - Confirm GRN
  type: http
  seq: 12
}

patch {
  url: {{host}}/api/{{bu_code}}/good-received-note/:id/confirm
  body: json
  auth: bearer
}

params:path {
  id: {{grn_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
