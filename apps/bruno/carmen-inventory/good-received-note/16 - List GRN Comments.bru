meta {
  name: 16 - List GRN Comments
  type: http
  seq: 16
}

get {
  url: {{host}}/api/{{bu_code}}/good-received-note/:id/comments
  body: none
  auth: bearer
}

params:path {
  id: {{grn_id}}
}

auth:bearer {
  token: {{access_token}}
}
