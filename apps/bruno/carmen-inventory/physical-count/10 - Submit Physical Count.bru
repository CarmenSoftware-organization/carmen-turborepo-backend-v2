meta {
  name: 10 - Submit Physical Count
  type: http
  seq: 10
}

patch {
  url: {{host}}/api/{{bu_code}}/physical-count/:id/submit
  body: none
  auth: bearer
}

params:path {
  id: {{physical_count_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  physical_count_id:
}
