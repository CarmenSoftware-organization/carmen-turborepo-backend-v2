meta {
  name: 09 - Get Review Result
  type: http
  seq: 9
}

get {
  url: {{host}}/api/{{bu_code}}/physical-count/:id/review
  body: none
  auth: bearer
}

params:path {
  id: {{physical_count_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  physical_count_id:
}
