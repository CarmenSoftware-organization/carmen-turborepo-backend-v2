meta {
  name: 05 - Delete Physical Count
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/physical-count/:id
  body: none
  auth: bearer
}

params:path {
  id: {{physical_count_id}}
}

auth:bearer {
  token: {{access_token}}
}
