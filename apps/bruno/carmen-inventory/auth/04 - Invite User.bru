meta {
  name: 04 - Invite User
  type: http
  seq: 4
}

post {
  url: {{host}}/api/auth/invite-user
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "email": ""
  }
}
