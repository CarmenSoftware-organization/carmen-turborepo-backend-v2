meta {
  name: 01.5 - Login GM
  type: http
  seq: 6
}

post {
  url: {{host}}/api/auth/login
  body: json
  auth: none
}

headers {
  x-app-id: {{x_app_id}}
}

body:json {
  {
    "email": "GM@blueledgers.com",
    "password": "12345678"
  }
}

script:post-response {
  bru.setEnvVar('access_token', res.body.data.access_token);
  bru.setEnvVar('refresh_token', res.body.data.refresh_token);
}
