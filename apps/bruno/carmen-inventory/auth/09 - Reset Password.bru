meta {
  name: 09 - Reset Password
  type: http
  seq: 9
}

post {
  url: {{host}}/api/auth/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "new_password": ""
  }
}
