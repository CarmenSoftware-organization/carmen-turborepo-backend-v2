meta {
  name: 02 - Logout
  type: http
  seq: 2
}

post {
  url: {{host}}/api/auth/logout
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
