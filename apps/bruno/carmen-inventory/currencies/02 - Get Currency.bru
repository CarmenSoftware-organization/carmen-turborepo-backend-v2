meta {
  name: 02 - Get Currency
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/currencies/:id
  body: none
  auth: bearer
}

params:path {
  id: {{currency_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
