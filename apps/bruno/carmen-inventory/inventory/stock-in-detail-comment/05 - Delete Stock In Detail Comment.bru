meta {
  name: Remove stock-in-detail-comment
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/stock-in-detail-comment/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

docs {
  ## Delete Stock In Detail Comment
  ลบความคิดเห็นของรายละเอียดการรับสินค้าเข้าคลัง

  Deletes a stock-in-detail comment. Only the author can delete.

  ### Path Parameters
  - `id` — Comment ID


  ### Sample Response
  ```json
  {
    "data": {
      "id": "uuid"
    },
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-03-27T00:00:00.000Z"
  }
  ```
}

params:path {
  id: {{test_id}}
}
