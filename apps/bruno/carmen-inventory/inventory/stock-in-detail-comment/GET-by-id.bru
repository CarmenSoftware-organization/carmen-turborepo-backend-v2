meta {
  name: By id stock-in-detail
  type: http
  seq: 9
}

get {
  url: {{host}}/api/{{bu_code}}/stock-in-detail/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
