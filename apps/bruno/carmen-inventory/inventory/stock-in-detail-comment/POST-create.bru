meta {
  name: Create stock-in-detail
  type: http
  seq: 10
}

post {
  url: {{host}}/api/{{bu_code}}/stock-in-detail
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
