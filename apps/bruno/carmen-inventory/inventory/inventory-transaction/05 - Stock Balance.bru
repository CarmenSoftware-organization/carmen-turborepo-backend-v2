meta {
  name: 05 - Stock Balance
  type: http
  seq: 5
}

docs {
  ## View Stock Balance
  ดูยอดคงเหลือสินค้ารวมตามสินค้าและสถานที่

  View aggregated stock balance per product/location.

  ### Query Parameters
  - `product_id` — Product UUID (optional)
}

get {
  url: {{host}}/api/{{bu_code}}/inventory-transaction/stock-balance
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
