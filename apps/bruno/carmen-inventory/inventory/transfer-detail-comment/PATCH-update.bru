meta {
  name: Update transfer-detail
  type: http
  seq: 11
}

patch {
  url: {{host}}/api/{{bu_code}}/transfer-detail/:id
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
