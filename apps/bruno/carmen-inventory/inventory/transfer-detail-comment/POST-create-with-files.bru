meta {
  name: Create with files transfer-detail-comment
  type: http
  seq: 13
}

post {
  url: {{host}}/api/{{bu_code}}/transfer-detail-comment/upload
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
