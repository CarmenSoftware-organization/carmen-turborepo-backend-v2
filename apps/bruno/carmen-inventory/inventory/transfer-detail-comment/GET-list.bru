meta {
  name: List transfer-detail
  type: http
  seq: 8
}

get {
  url: {{host}}/api/{{bu_code}}/transfer-detail
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
