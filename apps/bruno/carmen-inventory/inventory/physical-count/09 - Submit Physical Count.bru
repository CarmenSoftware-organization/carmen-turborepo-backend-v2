meta {
  name: Submit physical-count
  type: http
  seq: 9
}

patch {
  url: {{host}}/api/{{bu_code}}/physical-count/:id/submit
  body: none
  auth: bearer
}

params:query {
  ~version: 
  ~version: 
}

params:path {
  id: {{physical_count_id}}
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  physical_count_id: 267d6ba6-cf66-4b7f-927f-a7c52bfa68be
}

docs {
  ## Submit Physical Count
  ส่งการตรวจนับสินค้าขั้นสุดท้าย และสร้างรายการปรับปรุงสินค้าคงคลังอัตโนมัติเพื่อปรับจำนวนในระบบให้ตรงกับจำนวนที่นับได้จริง
  
  Finalizes a physical count and automatically generates inventory adjustment records to reconcile system stock levels with the actual counted quantities.
  
  ### Path Parameters
  - `bu_code` — Business Unit Code / รหัสหน่วยธุรกิจ
  - `id` — Physical Count ID / รหัสการตรวจนับสินค้า
  
  
  ### Sample Response
  ```json
  {
    "data": {
      "id": "uuid",
      "status": "counting"
    },
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-03-27T00:00:00.000Z"
  }
  ```
}
