meta {
  name: Find details by spot check id spot-check
  type: http
  seq: 12
}

get {
  url: {{host}}/api/{{bu_code}}/spot-check/:id/details
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
