meta {
  name: List spot-check
  type: http
  seq: 11
}

get {
  url: {{host}}/api/{{bu_code}}/spot-check
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
