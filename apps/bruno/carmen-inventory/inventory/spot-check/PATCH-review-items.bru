meta {
  name: Review items spot-check
  type: http
  seq: 15
}

patch {
  url: {{host}}/api/{{bu_code}}/spot-check/:id/review
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
