meta {
  name: Void stock out stock-out
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/{{bu_code}}/stock-out/:id/void
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
