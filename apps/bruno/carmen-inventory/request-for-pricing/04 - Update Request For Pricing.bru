meta {
  name: 04 - Update Request For Pricing
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/{{bu_code}}/request-for-pricing/:id
  body: json
  auth: bearer
}

params:path {
  id: {{rfp_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "",
    "note": ""
  }
}
