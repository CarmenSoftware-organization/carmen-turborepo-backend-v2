meta {
  name: 02 - Get Request For Pricing
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/request-for-pricing/:id
  body: none
  auth: bearer
}

params:path {
  id: {{rfp_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
