meta {
  name: 01 - List Report Types
  type: http
  seq: 1
}

docs {
  ## List Report Types
  แสดงรายการประเภทรายงานที่มีทั้งหมด

  Retrieve all available report types with metadata.
}

get {
  url: {{host}}/api/{{bu_code}}/report/types
  body: none
  auth: bearer
}

params:query {
  ~category:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
