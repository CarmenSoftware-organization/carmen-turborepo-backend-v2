meta {
  name: List schedules report
  type: http
  seq: 16
}

get {
  url: {{host}}/api/{{bu_code}}/report/schedules
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
