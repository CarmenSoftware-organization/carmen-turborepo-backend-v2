meta {
  name: 02 - Get Ingredient Units
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/unit/ingredient/product/:productId
  body: none
  auth: bearer
}

params:path {
  productId: {{product_id}}
}

auth:bearer {
  token: {{access_token}}
}
