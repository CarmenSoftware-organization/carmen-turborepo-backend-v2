meta {
  name: Swipe approve purchase-request
  type: http
  seq: 21
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-request/swipe-approve
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
