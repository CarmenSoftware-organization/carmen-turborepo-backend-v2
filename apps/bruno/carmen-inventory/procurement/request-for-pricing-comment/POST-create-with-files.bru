meta {
  name: Create with files request-for-pricing-comment
  type: http
  seq: 8
}

post {
  url: {{host}}/api/{{bu_code}}/request-for-pricing-comment/upload
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
