meta {
  name: Find all for grn purchase-order
  type: http
  seq: 23
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-order/grn
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
