meta {
  name: Submit purchase-order
  type: http
  seq: 24
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-order/:id/submit
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
