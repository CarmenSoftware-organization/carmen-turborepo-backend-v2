meta {
  name: Create with files credit-note-detail-comment
  type: http
  seq: 8
}

post {
  url: {{host}}/api/{{bu_code}}/credit-note-detail-comment/upload
  body: multipartForm
  auth: bearer
}

params:query {
  ~version: 
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
