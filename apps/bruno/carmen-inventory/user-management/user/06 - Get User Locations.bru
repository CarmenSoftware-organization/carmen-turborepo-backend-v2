meta {
  name: 06 - Get User Locations
  type: http
  seq: 6
}

docs {
  ## Get User Locations
  ดึงรายการสถานที่ของผู้ใช้ทั้งหมดที่ผู้ใช้ปัจจุบันเข้าถึงได้

  Retrieve all locations accessible by the current user.
}

get {
  url: {{host}}/api/{{bu_code}}/user-location
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
