meta {
  name: 01 - List Locations
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/locations
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
