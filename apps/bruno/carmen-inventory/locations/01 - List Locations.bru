meta {
  name: 01 - List Locations
  type: http
  seq: 2
}

docs {
  ## List Locations
  แสดงรายการสถานที่/คลังสินค้าทั้งหมดที่ผู้ใช้เข้าถึงได้

  Retrieves all locations/warehouses accessible to the current user.
}

get {
  url: {{host}}/api/{{bu_code}}/locations
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
