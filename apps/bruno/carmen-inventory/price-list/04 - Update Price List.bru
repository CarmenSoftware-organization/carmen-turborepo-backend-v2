meta {
  name: 04 - Update Price List
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/{{bu_code}}/price-list/:id
  body: json
  auth: bearer
}

params:path {
  id: {{price_list_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "",
    "note": ""
  }
}
