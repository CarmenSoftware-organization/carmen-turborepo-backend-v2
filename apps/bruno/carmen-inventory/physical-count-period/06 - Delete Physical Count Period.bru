meta {
  name: 06 - Delete Physical Count Period
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/{{bu_code}}/physical-count-period/:id
  body: none
  auth: bearer
}

params:path {
  id: {{physical_count_period_id}}
}

auth:bearer {
  token: {{access_token}}
}
