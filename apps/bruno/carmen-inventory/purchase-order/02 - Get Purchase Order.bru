meta {
  name: 02 - Get Purchase Order
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-order/:id
  body: none
  auth: bearer
}

params:path {
  id: {{po_id}}
}

auth:bearer {
  token: {{access_token}}
}
