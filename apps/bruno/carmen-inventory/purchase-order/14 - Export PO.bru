meta {
  name: 14 - Export PO
  type: http
  seq: 14
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-order/:id/export
  body: none
  auth: bearer
}

params:path {
  id: {{po_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
