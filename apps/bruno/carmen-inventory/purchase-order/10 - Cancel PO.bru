meta {
  name: 10 - Cancel PO
  type: http
  seq: 10
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-order/:id/cancel
  body: json
  auth: bearer
}

params:path {
  id: {{po_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
