meta {
  name: 03 - Create Purchase Order
  type: http
  seq: 3
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-order
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "",
    "note": ""
  }
}
