meta {
  name: 12 - Group PR
  type: http
  seq: 12
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-order/group-pr
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
