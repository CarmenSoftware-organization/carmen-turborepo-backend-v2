meta {
  name: 06 - Submit SR
  type: http
  seq: 6
}

patch {
  url: {{host}}/api/{{bu_code}}/store-requisition/:id/submit
  body: json
  auth: bearer
}

params:path {
  id: {{sr_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
