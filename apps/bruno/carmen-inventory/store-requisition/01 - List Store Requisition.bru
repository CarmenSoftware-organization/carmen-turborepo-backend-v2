meta {
  name: 01 - List Store Requisition
  type: http
  seq: 1
}

get {
  url: {{host}}/api/store-requisition
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
