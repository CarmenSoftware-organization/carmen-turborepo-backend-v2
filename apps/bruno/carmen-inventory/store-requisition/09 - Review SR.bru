meta {
  name: 09 - Review SR
  type: http
  seq: 9
}

patch {
  url: {{host}}/api/{{bu_code}}/store-requisition/:id/review
  body: json
  auth: bearer
}

params:path {
  id: {{sr_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
