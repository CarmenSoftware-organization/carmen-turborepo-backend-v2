meta {
  name: 04 - Update Store Requisition
  type: http
  seq: 4
}

put {
  url: {{host}}/api/{{bu_code}}/store-requisition/:id
  body: json
  auth: bearer
}

params:path {
  id: {{sr_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "",
    "note": ""
  }
}
