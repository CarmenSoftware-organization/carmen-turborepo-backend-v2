meta {
  name: Get product cost products
  type: http
  seq: 4
}

post {
  url: {{host}}/api/{{bu_code}}/products/cost
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
