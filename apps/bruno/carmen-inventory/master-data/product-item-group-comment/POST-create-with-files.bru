meta {
  name: Create with files product-item-group-comment
  type: http
  seq: 8
}

post {
  url: {{host}}/api/{{bu_code}}/product-item-group-comment/upload
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
