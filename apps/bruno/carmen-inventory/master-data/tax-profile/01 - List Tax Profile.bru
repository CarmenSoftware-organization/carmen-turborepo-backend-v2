meta {
  name: 01 - List Tax Profile
  type: http
  seq: 1
}

docs {
  ## List Tax Profiles
  แสดงรายการโปรไฟล์ภาษีทั้งหมดพร้อมการแบ่งหน้าและค้นหา

  Lists all tax configuration profiles available in the business unit with pagination and search support.
}

get {
  url: {{host}}/api/{{bu_code}}/tax-profile
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
