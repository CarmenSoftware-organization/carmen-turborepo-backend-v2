meta {
  name: 03 - Create PR Comment
  type: http
  seq: 3
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-request-comment
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "purchase_request_id": "{{pr_id}}",
    "comment": ""
  }
}
