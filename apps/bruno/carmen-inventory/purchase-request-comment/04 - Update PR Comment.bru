meta {
  name: 04 - Update PR Comment
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request-comment/:id
  body: json
  auth: bearer
}

params:path {
  id: {{pr_comment_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "comment": ""
  }
}
