meta {
  name: 01 - List PR Template filter
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-request-template?search=sss
  body: none
  auth: bearer
}

params:query {
  search: sss
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
