meta {
  name: 05 - Delete Spot Check
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/spot-check/:id
  body: none
  auth: bearer
}

params:path {
  id: {{spot_check_id}}
}

auth:bearer {
  token: {{access_token}}
}
