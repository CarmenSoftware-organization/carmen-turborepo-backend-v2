meta {
  name: 07 - Submit Pending PR
  type: http
  seq: 7
}

patch {
  url: {{host}}/api/my-pending/purchase-request/:bu_code/:id/submit
  body: json
  auth: bearer
}

params:path {
  bu_code: {{bu_code}}
  id: {{pr_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
