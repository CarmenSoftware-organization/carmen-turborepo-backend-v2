meta {
  name: Reset user password platform-user
  type: http
  seq: 2
}

put {
  url: {{host}}/api-system/user/:id/reset-password
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
