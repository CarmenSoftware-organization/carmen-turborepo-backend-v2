meta {
  name: 05 - Delete User Business Unit
  type: http
  seq: 5
}

docs {
  ## Delete User Business Unit
  ลบหน่วยธุรกิจของผู้ใช้ตาม ID

  Delete a user-business unit mapping by its ID.
}

delete {
  url: {{host}}/api-system/user/business-unit/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
