meta {
  name: 04 - Update User
  type: http
  seq: 4
}

docs {
  ## Update User
  อัปเดตข้อมูลผู้ใช้ที่มีอยู่

  Update an existing user.
}

put {
  url: {{host}}/api-system/user/:id
  body: json
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "",
    "email": ""
  }
}
