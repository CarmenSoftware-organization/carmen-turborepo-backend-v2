meta {
  name: 01 - List User
  type: http
  seq: 1
}

docs {
  ## List User
  แสดงรายการผู้ใช้ทั้งหมดพร้อมการแบ่งหน้าและค้นหา

  Retrieve all users with pagination and search support.
}

get {
  url: {{host}}/api-system/user
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
