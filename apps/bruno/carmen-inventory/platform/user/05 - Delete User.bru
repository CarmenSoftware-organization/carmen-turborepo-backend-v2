meta {
  name: 05 - Delete User
  type: http
  seq: 5
}

docs {
  ## Delete User
  ลบผู้ใช้ตาม ID

  Delete a user by their ID.
}

delete {
  url: {{host}}/api-system/user/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
