meta {
  name: 06 - Fetch User
  type: http
  seq: 6
}

post {
  url: {{host}}/api-system/user/fetch-user
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
