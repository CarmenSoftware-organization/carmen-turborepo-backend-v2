meta {
  name: Create platform_report-template
  type: http
  seq: 3
}

post {
  url: {{host}}/api-system/report-template
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
