meta {
  name: Update platform_report-template
  type: http
  seq: 4
}

put {
  url: {{host}}/api-system/report-template/:id
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}

body:json {
  {}
}
