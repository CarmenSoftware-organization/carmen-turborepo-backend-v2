meta {
  name: By id platform_report-template
  type: http
  seq: 2
}

get {
  url: {{host}}/api-system/report-template/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
