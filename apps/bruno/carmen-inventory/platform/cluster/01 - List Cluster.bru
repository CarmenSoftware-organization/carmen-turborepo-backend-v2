meta {
  name: 01 - List Cluster
  type: http
  seq: 1
}

docs {
  ## List Cluster
  แสดงรายการคลัสเตอร์ทั้งหมดพร้อมการแบ่งหน้าและค้นหา

  Retrieve all clusters with pagination and search support.
}

get {
  url: {{host}}/api-system/cluster
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
