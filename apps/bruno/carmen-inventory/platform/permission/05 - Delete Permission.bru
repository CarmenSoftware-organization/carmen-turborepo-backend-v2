meta {
  name: 05 - Delete Permission
  type: http
  seq: 5
}

delete {
  url: {{host}}/api-system/permission/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
