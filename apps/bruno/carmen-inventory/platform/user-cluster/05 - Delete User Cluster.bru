meta {
  name: 05 - Delete User Cluster
  type: http
  seq: 5
}

docs {
  ## Delete User Cluster
  ลบคลัสเตอร์ของผู้ใช้ตาม ID

  Delete a user-cluster mapping by its ID.
}

delete {
  url: {{host}}/api-system/user/cluster/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
