meta {
  name: 05 - Delete User Cluster
  type: http
  seq: 5
}

delete {
  url: {{host}}/api-system/user/cluster/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
