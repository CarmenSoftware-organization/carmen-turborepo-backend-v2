meta {
  name: 02 - Get Transfer
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/transfer/:id
  body: none
  auth: bearer
}

params:path {
  id: {{transfer_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
