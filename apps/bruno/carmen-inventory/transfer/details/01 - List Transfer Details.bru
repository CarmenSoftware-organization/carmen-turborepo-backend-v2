meta {
  name: 01 - List Transfer Details
  type: http
  seq: 1
}

get {
  url: {{host}}/api/{{bu_code}}/transfer/:id/details
  body: none
  auth: bearer
}

params:path {
  id: {{transfer_id}}
}

auth:bearer {
  token: {{access_token}}
}
