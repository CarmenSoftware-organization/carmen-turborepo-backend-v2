meta {
  name: 05 - Delete Transfer
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/transfer/:id
  body: none
  auth: bearer
}

params:path {
  id: {{transfer_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
