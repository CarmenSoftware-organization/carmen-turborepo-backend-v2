meta {
  name: 08 - Submit Pending SR
  type: http
  seq: 8
}

patch {
  url: {{host}}/api/my-pending/store-requisition/:bu_code/:id/submit
  body: json
  auth: bearer
}

params:path {
  bu_code: {{bu_code}}
  id: {{sr_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
