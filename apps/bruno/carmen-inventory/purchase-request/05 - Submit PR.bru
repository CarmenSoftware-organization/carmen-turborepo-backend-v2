meta {
  name: 05 - Submit PR
  type: http
  seq: 5
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/submit
  body: json
  auth: bearer
}

params:path {
  id: {{pr_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
