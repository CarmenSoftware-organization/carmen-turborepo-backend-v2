meta {
  name: 15 - Get PR Status
  type: http
  seq: 15
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/status/:status
  body: none
  auth: bearer
}

params:path {
  id: {{pr_id}}
  status: draft
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
