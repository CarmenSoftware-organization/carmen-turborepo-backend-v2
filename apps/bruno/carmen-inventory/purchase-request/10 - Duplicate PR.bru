meta {
  name: 10 - Duplicate PR
  type: http
  seq: 10
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-request/duplicate-pr
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "id": "{{pr_id}}"
  }
}
