meta {
  name: 07 - Reject PR
  type: http
  seq: 7
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/reject
  body: json
  auth: bearer
}

params:path {
  id: {{pr_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
