meta {
  name: 11 - Split PR
  type: http
  seq: 11
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/split
  body: json
  auth: bearer
}

params:path {
  id: {{pr_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
