meta {
  name: 08 - Review PR
  type: http
  seq: 8
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/review
  body: json
  auth: bearer
}

params:path {
  id: {{pr_id}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
