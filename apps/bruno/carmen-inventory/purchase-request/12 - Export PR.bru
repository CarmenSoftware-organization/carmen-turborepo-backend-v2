meta {
  name: 12 - Export PR
  type: http
  seq: 12
}

get {
  url: {{host}}/api/{{bu_code}}/purchase-request/:id/export
  body: none
  auth: bearer
}

params:path {
  id: {{pr_id}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
