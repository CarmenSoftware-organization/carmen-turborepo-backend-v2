meta {
  name: Multipart upload
  type: http
  seq: 50
}

post {
  url: {{host}}/api/upload
  body: multipart-form
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

body:multipart-form {
  ~file:
}
